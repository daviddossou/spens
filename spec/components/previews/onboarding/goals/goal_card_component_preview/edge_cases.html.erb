<%
  grid_class = case columns || 3
              when 1 then 'grid grid-1'
              when 2 then 'grid grid-2'
              when 3 then 'grid grid-3'
              when 4 then 'grid grid-4'
              else 'grid grid-auto-fit'
              end
%>

<div class="preview-info mb-4">
  <p>🧪 Edge Cases Demo: Testing various content scenarios including empty descriptions, long names, special characters, unicode emojis, and extremely long descriptions.</p>
  <p>Pre-selected: Special & Characters, 💰 Wealth Building 🏠</p>
</div>

<div class="<%= grid_class %>">
  <% goals.each do |goal| %>
    <%= render Onboarding::Goals::GoalCardComponent.new(
      item: {
        key: goal[:key],
        name: goal[:name],
        description: goal[:description]
      },
      form: mock_form,
      model: mock_model
    ) %>
  <% end %>
</div>
