<%= form_with model: space, url: "#", data: { controller: "onboarding--financial-goals" } do |f| %>
  <%= render Onboarding::FinancialGoals::SelectableCardComponent.new(
    item: goal,
    form: f,
    model: space
  ) %>
<% end %>
