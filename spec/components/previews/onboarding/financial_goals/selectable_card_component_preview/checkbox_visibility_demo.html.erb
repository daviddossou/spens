<%= form_with model: user, url: "#", data: { controller: "onboarding--financial-goals" } do |f| %>
  <div class="checkbox-demo">
    <div class="preview-info mb-4">
      <p>👁️ Visual Checkbox Demo: The left card has no selection (hidden checkbox), the right card is pre-selected (visible checkbox).</p>
    </div>

    <div class="grid grid-2">
      <% goals.each do |goal| %>
        <%= render Onboarding::FinancialGoals::SelectableCardComponent.new(
          item: ActiveSupport::HashWithIndifferentAccess.new(goal),
          form: f,
          model: user
        ) %>
      <% end %>
    </div>
  </div>
<% end %>
