<%= form_with model: space, url: "#", data: { controller: "onboarding--financial-goals" } do |f| %>
  <div class="grid grid-2">
    <% goals.each do |goal| %>
      <%= render Onboarding::FinancialGoals::SelectableCardComponent.new(
        item: ActiveSupport::HashWithIndifferentAccess.new(goal),
        form: f,
        model: space
      ) %>
    <% end %>
  </div>

  <div class="preview-info mt-4">
    <p>💡 This preview shows mixed selection states - some goals are pre-selected to demonstrate the visual difference.</p>
    <p>Selected: Save regularly, Track all accounts</p>
  </div>
<% end %>
