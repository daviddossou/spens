<div class="max-w-xl mx-auto p-8">
  <h2 class="text-2xl font-bold mb-6">Input Field with Both Addons</h2>

  <p class="text-gray-600 mb-4">
    Example of a field with both prepend and append addons.
  </p>

  <%= form_with model: User.new, url: "#" do |f| %>
    <%= render Forms::InputFieldComponent.new(
      form: f,
      field: :price,
      type: :number_field,
      label: "Price Range",
      help_text: "Enter the price with currency and unit",
      prepend: "$",
      append: "USD",
      required: true,
      field_options: { step: "0.01", min: "0" }
    ) %>

    <div class="mt-4">
      <%= f.submit "Submit", class: "px-4 py-2 bg-blue-500 text-white rounded" %>
    </div>
  <% end %>

  <div class="mt-6 p-4 bg-gray-50 rounded">
    <h3 class="font-semibold mb-2">💡 Features:</h3>
    <ul class="list-disc list-inside text-sm text-gray-700 space-y-1">
      <li>Currency symbol on the left</li>
      <li>Currency code on the right</li>
      <li>Input field in the middle</li>
      <li>Fully integrated visual design</li>
    </ul>
  </div>
</div>
