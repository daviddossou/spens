<div class="max-w-xl mx-auto p-8">
  <h2 class="text-2xl font-bold mb-6">Disabled Input Field</h2>

  <p class="text-gray-600 mb-4">
    Example of a disabled field that cannot be edited.
  </p>

  <%= form_with model: User.new, url: "#" do |f| %>
    <%= render Forms::InputFieldComponent.new(
      form: f,
      field: :readonly_field,
      label: "Read-only Field",
      help_text: "This field is disabled and cannot be edited",
      field_options: { disabled: true, value: "This value cannot be changed" }
    ) %>

    <%= render Forms::InputFieldComponent.new(
      form: f,
      field: :amount,
      type: :number_field,
      label: "Calculated Amount",
      help_text: "This amount is automatically calculated",
      prepend: "XOF",
      field_options: { disabled: true, value: "5000" }
    ) %>

    <div class="mt-4">
      <%= f.submit "Submit", class: "px-4 py-2 bg-blue-500 text-white rounded" %>
    </div>
  <% end %>

  <div class="mt-6 p-4 bg-gray-50 rounded">
    <h3 class="font-semibold mb-2">💡 Features:</h3>
    <ul class="list-disc list-inside text-sm text-gray-700 space-y-1">
      <li>Grayed out appearance</li>
      <li>Non-editable state</li>
      <li>Works with addons (prepend/append)</li>
      <li>Clear visual indicator of disabled state</li>
    </ul>
  </div>
</div>
