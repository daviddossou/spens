<div class="max-w-xl mx-auto p-8">
  <h2 class="text-2xl font-bold mb-6">Autocomplete - <%= model_name %> Templates</h2>

  <p class="text-gray-600 mb-4">
    Testing with real <%= model_name %> template suggestions from i18n. Only expense types are shown.
  </p>

  <div class="mb-4 p-3 bg-purple-50 rounded text-sm">
    <strong>Total expense types:</strong> <%= suggestions.size %> templates loaded from <code>config/locales/en/transaction_type_templates.yml</code>
  </div>

  <%= form_with model: TransactionType.new, url: "#", method: :post do |form| %>
    <%= render Forms::InputFieldComponent.new(
      form: form,
      field: :name,
      label: "Expense Type",
      placeholder: "Type or select expense type...",
      autocomplete: true,
      autocomplete_options: suggestions,
      allow_create: allow_create,
      required: true
    ) %>

    <%= render Forms::InputFieldComponent.new(
      form: form,
      field: :amount,
      type: :number_field,
      placeholder: "0.00",
      step: "0.01"
    ) %>

    <div class="mt-4">
      <%= form.submit "Add Expense", class: "px-4 py-2 bg-blue-500 text-white rounded" %>
    </div>
  <% end %>

  <div class="mt-6 p-4 bg-red-50 rounded">
    <h3 class="font-semibold mb-2">💡 Try these:</h3>
    <ul class="list-disc list-inside text-sm text-gray-700 space-y-1">
      <li>Type "rent" to see Rent</li>
      <li>Type "food" to see Food / Restaurant</li>
      <li>Type "transport" to see Fuel & Transport</li>
      <li>Type "shopping" to filter shopping categories</li>
      <li>Add your own custom expense type!</li>
    </ul>
  </div>
</div>
