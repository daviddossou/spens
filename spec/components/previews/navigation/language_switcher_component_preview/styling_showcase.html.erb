<% content_for :preview_title, "Language Switcher - Styling Showcase" %>

<div class="space-y-8">
  <p class="text-gray-600">
    Showcase of different states and configurations for the LanguageSwitcherComponent.
  </p>

  <% components.each do |item| %>
    <div class="border border-gray-200 rounded-lg p-6 bg-gray-50">
      <div class="mb-4">
        <h4 class="text-sm font-semibold text-gray-900 mb-2">
          <%= item[:title] %>
        </h4>
      </div>

      <div class="flex items-center space-x-4">
        <%= render item[:component] %>
      </div>
    </div>
  <% end %>

  <div class="mt-8 p-4 bg-blue-50 border border-blue-200 rounded-lg">
    <h4 class="text-sm font-medium text-blue-900 mb-2">Usage Example</h4>
    <pre class="text-xs text-blue-800 overflow-x-auto"><code>&lt;%= render Navigation::LanguageSwitcherComponent.new(
  current_locale: I18n.locale,
  available_locales: I18n.available_locales,
  params: params
) %&gt;</code></pre>
  </div>

  <div class="mt-4 p-4 bg-green-50 border border-green-200 rounded-lg">
    <h4 class="text-sm font-medium text-green-900 mb-2">Features</h4>
    <ul class="text-xs text-green-800 space-y-1">
      <li>✓ Automatic styling based on current locale</li>
      <li>✓ Preserves existing URL parameters</li>
      <li>✓ Hover states for better UX</li>
      <li>✓ Handles edge cases (nil locale, empty arrays)</li>
      <li>✓ Works with ActionController::Parameters</li>
    </ul>
  </div>
</div>
