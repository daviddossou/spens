<%= form_with model: User.new, url: "#" do |form| %>
  <%= render Ui::SelectableCardComponent.new(
    item: "Simple String Option",
    form: form,
    field: :preferences,
    selected: false
  ) %>
<% end %>
