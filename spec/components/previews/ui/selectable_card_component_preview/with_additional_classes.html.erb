<div class="max-w-2xl mx-auto p-8">
  <h2 class="text-2xl font-bold mb-6">Additional Classes Example</h2>

  <p class="text-gray-600 mb-4">
    The <code>additional_classes</code> parameter allows adding extra CSS classes without replacing the base "card" class.
  </p>

  <div class="mb-4 p-3 bg-blue-50 rounded text-sm space-y-2">
    <div><strong>Default:</strong> <code>css_class: "card"</code></div>
    <div><strong>Additional:</strong> <code>additional_classes: "custom-style"</code></div>
    <div><strong>Result:</strong> <code>class="card custom-style selected"</code></div>
  </div>

  <%= form_with url: "#", method: :post do |form| %>
    <div class="space-y-4">
      <!-- Default card (no additional classes) -->
      <div>
        <h3 class="text-sm font-semibold mb-2">Default Card</h3>
        <%= render Ui::SelectableCardComponent.new(
          item: { name: "Default Style", value: "default", description: "Uses base 'card' class only" },
          form: form,
          field: :style_test,
          selected: false,
          multiple: false
        ) %>
      </div>

      <!-- Card with additional classes for centering -->
      <div>
        <h3 class="text-sm font-semibold mb-2">With Additional Classes (centered text)</h3>
        <%= render Ui::SelectableCardComponent.new(
          item: { name: "Centered Style", value: "centered", description: "Added 'text-center' class" },
          form: form,
          field: :style_test,
          selected: false,
          multiple: false,
          additional_classes: 'text-center'
        ) %>
      </div>

      <!-- Card with multiple additional classes -->
      <div>
        <h3 class="text-sm font-semibold mb-2">Multiple Additional Classes</h3>
        <%= render Ui::SelectableCardComponent.new(
          item: { name: "Custom Styled", value: "custom", description: "Multiple classes: border-2 border-blue-500 shadow-lg" },
          form: form,
          field: :style_test,
          selected: true,
          multiple: false,
          additional_classes: 'border-2 border-blue-500 shadow-lg'
        ) %>
      </div>
    </div>
  <% end %>

  <div class="mt-6 p-4 bg-gray-50 rounded">
    <h4 class="font-semibold mb-2">Why use additional_classes?</h4>
    <ul class="text-sm space-y-1">
      <li>✅ Keeps base "card" class (maintains component styling)</li>
      <li>✅ Adds custom classes (e.g., text-center, custom-border)</li>
      <li>✅ Backwards compatible (doesn't break existing uses)</li>
      <li>⚠️ Don't use <code>css_class</code> for this - it replaces the base class</li>
    </ul>
  </div>
</div>
