<div class="max-w-xl mx-auto p-8">
  <h2 class="text-2xl font-bold mb-6">Submit Buttons in Forms</h2>

  <p class="text-gray-600 mb-4">
    Examples of submit buttons with different variants and states.
  </p>

  <div class="space-y-6">
    <h3 class="font-semibold text-gray-700">Basic Submit Button</h3>
    <%= form_with url: "#", method: :post do %>
      <div class="mb-4">
        <label class="block text-sm font-medium mb-2">Username</label>
        <%= text_field_tag :username, nil, class: "form-control" %>
      </div>
      <%= render Ui::ButtonComponent.new(type: :submit, variant: :primary) do %>
        Save Changes
      <% end %>
    <% end %>

    <h3 class="font-semibold text-gray-700 mt-6">Submit with Form Attribute</h3>
    <%= form_with url: "#", id: "external-form", method: :post do %>
      <div class="mb-4">
        <label class="block text-sm font-medium mb-2">Email</label>
        <%= email_field_tag :email, nil, class: "form-control" %>
      </div>
    <% end %>
    <div class="mt-4">
      <%= render Ui::ButtonComponent.new(
        type: :submit,
        form: "external-form",
        variant: :success
      ) do %>
        Submit External Form
      <% end %>
    </div>

    <h3 class="font-semibold text-gray-700 mt-6">Multiple Submit Buttons</h3>
    <%= form_with url: "#", method: :post do %>
      <div class="mb-4">
        <label class="block text-sm font-medium mb-2">Comment</label>
        <%= text_area_tag :comment, nil, rows: 3, class: "form-control" %>
      </div>
      <div class="flex gap-2">
        <%= render Ui::ButtonComponent.new(type: :submit, variant: :primary) do %>
          Submit
        <% end %>
        <%= render Ui::ButtonComponent.new(type: :submit, variant: :secondary) do %>
          Save as Draft
        <% end %>
        <%= render Ui::ButtonComponent.new(type: :button, variant: :outline_secondary) do %>
          Cancel
        <% end %>
      </div>
    <% end %>

    <h3 class="font-semibold text-gray-700 mt-6">Loading State on Submit</h3>
    <%= form_with url: "#", method: :post, data: { turbo: false } do %>
      <div class="mb-4">
        <label class="block text-sm font-medium mb-2">Data</label>
        <%= text_field_tag :data, nil, class: "form-control" %>
      </div>
      <%= render Ui::ButtonComponent.new(type: :submit, variant: :primary, loading: true) do %>
        Processing...
      <% end %>
    <% end %>
  </div>

  <div class="mt-6 p-4 bg-gray-50 rounded">
    <h3 class="font-semibold mb-2">💡 Tips:</h3>
    <ul class="list-disc list-inside text-sm text-gray-700 space-y-1">
      <li>Use <code>type: :submit</code> for form submission buttons</li>
      <li>Use <code>form: "form-id"</code> to submit forms outside the button</li>
      <li>Use <code>type: :button</code> for non-submit actions within forms</li>
      <li>Multiple submit buttons can have different values/names</li>
      <li>Set <code>loading: true</code> to show spinner state during processing</li>
    </ul>
  </div>
</div>
