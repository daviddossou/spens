<%= t('invitations.mailer.greeting') %>

<%= t('invitations.mailer.message', name: @invited_by.first_name.presence || @invited_by.email.split("@").first, space_name: @space.name) %>

<%= t('invitations.mailer.accept_button') %>: <%= @accept_url %>

<%= t('invitations.mailer.ignore') %>
