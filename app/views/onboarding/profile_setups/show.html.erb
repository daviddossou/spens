<% content_for :title, t('.title') %>

<div class="onboarding-step">
  <%= render Onboarding::StepHeaderComponent.new(step: 2, title: t('.title')) %>

  <div class="onboarding-step__body">
    <p class="onboarding-step__lead"><%= t('.subtitle') %></p>

    <%= render 'form' %>
  </div>
</div>
