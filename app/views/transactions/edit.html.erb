<% content_for :title, t('.title') %>

<%= turbo_frame_tag "modal" do %>
  <div class="page-container page-container--narrow">
    <%= turbo_frame_tag "transaction_form" do %>
      <%= render Ui::PageHeaderComponent.new(classes: nil) do %>
        <p class="page-header__subtitle"><%= t('.subtitle') %></p>
      <% end %>

      <div class="page-content">
        <%= render "form",
              form_url: transaction_path(id: @form.transaction.id),
              form_method: :patch %>
      </div>
    <% end %>
  </div>
<% end %>
