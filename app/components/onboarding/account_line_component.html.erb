<div class="account-line" data-onboarding--account-setup-target="accountLine">
  <div class="account-line__fields">
    <div class="account-line__field">
      <%= render Forms::PickerFieldComponent.new(
        form: form,
        field: :account_name,
        label: t('.account_name'),
        title: t('picker.account'),
        placeholder: t('.account_name_placeholder'),
        help_text: t('.account_help'),
        rows: account_rows,
        allow_create: true
      ) %>
    </div>

    <div class="account-line__amount-field">
      <%= render Forms::InputFieldComponent.new(
        form: form,
        field: :amount,
        type: :number_field,
        label: t('.initial_amount'),
        required: true,
        help_text: t('.amount_help'),
        prepend: @currency,
        step: "0.01",
        min: "0"
      ) %>
    </div>

    <%= form.hidden_field :transaction_date, value: Date.current %>
    <%= form.hidden_field :transaction_type_name, value: I18n.t("transactions.initial_balance.type_name") %>
    <%= form.hidden_field :transaction_type_kind, value: Onboarding::TransactionForm::DEFAULT_TRANSACTION_TYPE_KIND %>
  </div>

  <% if can_remove %>
    <div class="account-line__remove-link">
      <button type="button"
              class="link-button link-danger"
              data-action="click->onboarding--account-setup#removeLine">
        <%= t('.remove') %>
      </button>
    </div>
  <% end %>
</div>
